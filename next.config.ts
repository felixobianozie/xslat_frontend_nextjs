import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ["10.150.175.122"], // Allow access on my local network IP
};

export default nextConfig;
